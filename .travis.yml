dist: xenial
sudo: required

language: python
python:
  - "2.7"
  - "3.6"
  - "3.7"

# command to install dependencies
install:
  - pip install tox-travis

# command to run tests
script:
  tox


notifications:
  email:
    on_success: never
